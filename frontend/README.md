# Frontend - Smart Task Manager

## Development (local)
1. Install Node.js
2. From frontend directory:
   ```bash
   npm install
   npm start
   ```
3. By default it expects backend at `http://localhost:4000/api`. You can override by setting `REACT_APP_API_URL`.

## Build (production)
```bash
npm run build
```
